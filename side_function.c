#include "sorting.h"
#include <stdio.h>
#include <string.h>
void read_num_rand(int *array){
	int i=0;
	while(scanf("%s", array[i]) !=EOF)
		;
}
void read_eng_rand(char **array){
	int i=0;
	while(scanf("%s", array[i]) !=EOF)
		;
}
